
A｜逻辑与过渡（Pass A）
用途：补导语、桥接句、小结；消除跳跃与重复。
请在不破坏 LaTeX 结构的前提下，优化以下片段的“逻辑连贯性与过渡”：
- 找出逻辑跳跃/重复/缺少承上启下的位置；
- 为每处补 1–2 句 “过渡/导语/小结”；
- 对每小节补一句“导航句”（本节将…、接下来我们…）；
- 保留 \label \ref \cite 与环境结构；
输出：①修改后片段 ②修改点清单 ③可选的段落级小标题建议（最多3条）。
【待处理】：



B｜术语与数学一致性（Pass B）
用途：校术语、符号、定义、公式前后语义。
请对以下 LaTeX 片段做“一致性审校”，只给出问题与建议，不直接改结构：
- 术语是否统一（中/英/译法/首次出现处）→ 给“术语对照表候选”；
- 符号是否统一（θ/φ/λ、向量粗体、集合黑板体等）→ 给“符号对照表候选”；
- 公式是否前后语义连贯（是否需要动机/定义/结论补句）→ 列出位置与建议；
输出：①问题定位清单（按段/公式编号）②术语表候选 ③符号表候选 ④需作者确认项。
【待处理】：



C｜教学化增强（Pass C）
用途：提问、例子、互动题，让内容“会教人”。
在不改变 LaTeX 命令/结构的前提下，为此片段添加教学元素：
- 启发式问题 3 个（Why/What if/How 类型，短而有力）；
- 生活化 mini case 1 个（≤120字，贴近本节概念）；
- 课堂互动题 1 个（开放题，含讨论要点提纲3-4条）；
- 若合适，给出“常见误解/易错点” 2 条；
输出：可直接粘贴的 LaTeX 片段（用 \paragraph 或 \subsubsection 小标题组织），并附提纲版清单。
【待处理】：



D｜行文与格式（Pass D）
用途：节奏、冗余、版式溢出风险提示。
请进行“可读性压缩与节奏优化”，不改 LaTeX 命令/结构：
- 合并/拆分长句，避免叠词与赘述；
- 标注可能造成 Overfull \hbox 的超长公式/长词（给替代表达）；
- 对图表题注给统一风格建议（句式模板与长度）；
输出：①优化后片段（仅正文）②压缩率估计（%）③潜在版式风险点（行号+建议）。
【待处理】：



图表与代码块生成
图注/表注统一模板
请为以下图/表生成统一风格题注（≤25字，含信息点1-2个），并给出可粘贴 LaTeX：
- 图题：\caption{…}；表题：\caption{…}
- 若需引用处，附“推荐引用句”1句（含 \cref{fig:...}/\cref{tab:...}）。
输入：图/表关键信息要点（项目符号）



术语/符号抽取与维护
请从以下片段抽取“术语表”和“符号表”：
- 术语表字段：中文|英文|定义（≤20字）|首次出现位置；
- 符号表字段：符号|含义|单位/域|首次出现位置|是否需统一；
输出两个表（Markdown 或 LaTeX tabular），并列出“需作者确认项”。
【待处理】：



交叉引用与结构检查
请检查此片段的 \label/\ref/\cite 使用是否规范：
- 是否存在未定义引用、同名 label、跨章命名冲突；
- 建议命名规范（sec: / fig: / tab: / eq: 前缀）；
- 如需重命名，请给“替换映射表”，不直接改原文。
输出：①问题清单 ②命名规范建议 ③替换映射表（仅建议）。
【待处理】：



翻译与双语平衡（选用）
技术中文→学术英文（保持 LaTeX）
请将以下中文技术段落译为学术英文：
- 保留 LaTeX 数学环境与命令；
- 术语采用主流 AI/ML 论文写法；
- 句式简洁，适合教材；
输出：英文版正文（可直接粘贴）。
【待处理】：



章节提要 & 结尾小结
基于以下章节内容，生成：
- 开篇提要（3-4句，说明“本章解决什么/学完能做什么/先修要求”）；
- 结尾小结（3-4句，回顾要点+下一章衔接语1句）；
- 关键术语清单（≤8条）。
输出：可直接粘贴的 LaTeX 片段（\section*{本章提要}/\section*{小结}）。
【待处理】：



出版配套（提案/封底/PPT 大纲）
封底简介（200字）
请用亲切而有学术气质的语气，为《人工智能的数学思维》撰写封底简介（约200字）：
- 目标读者：本科生与工程从业者；
- 特色：思维导向、案例启发、数学严谨不过载；
- 避免空话套话，突出“学完能做什么”。


章节→PPT 大纲（15页）
请将以下小节内容转换为 15 页教学 PPT 大纲：
- 每页：标题 + 3~5个要点（含关键公式/图示建议）；
- 标明“互动/思考/示例”页；
- 给出黑板推导页（公式编号或关键步骤）。
【待处理】：



Pandoc 中转（大改时）
tex→md 强改→tex 回写的专用提示
以下是从 LaTeX 转出的 Markdown 片段。请在不改变数学块 $$…$$ 的前提下进行“大幅重写”（逻辑与教学性），输出仍为 Markdown（保持 ## 层级与公式块原样）。不要引入新图片路径与文件名。
【待处理】：



Commit 信息生成（可选）
请基于本次改动说明，生成 3 条 Git commit 信息（50 字符内，英文或中文），风格为：
- ch3: passA logic bridges + summary
- ch3: passB symbols/terms map
- ch3: passC questions+mini-case
输入：这次修改做了什么（项目符号）



一句话速用版（复制到对话框的短句）
* 逻辑：不改 LaTeX 结构，补过渡/导语/小结，给修改后片段+修改点。
* 一致性：列出术语/符号/公式语义问题与建议表，不直接改代码。
* 教学化：加3问+1例+1互动题，返回可粘贴 LaTeX 片段。
* 格式：做可读性压缩，标注潜在 Overfull 风险并给替代表达。
* 术语表：抽取术语/符号表（含首次出现位置），列出需确认项。
* PPT：把本节转 15页 PPT 大纲，标注互动/推导页。


# 第6次提示词处理：修复源码命令渲染

## 时间
2026-06-25 17:44

## 请求方
前端app

## 本次任务重点
修复渲染页面中常见 LaTeX 命令直接暴露的问题。

## 下一步行动
- 重新生成当前章节页面，确认标题中的 `texorpdfstring` 不再出现。
- 检查引语命令 `cnquote` 是否显示为可读文本。
- 后续如遇更多自定义命令，再加入渲染器的轻量白名单。

## 前端app
- 增加 LaTeX inline 归一化逻辑，支持 `texorpdfstring`、`cnquote`、`href`、`textcolor`、`url`、`footnote` 等常见命令。
- 渲染层只改变 HTML 展示，不修改源 tex。

## 管理后台
- 暂无管理后台改动。

## 后端
- 暂无后端接口改动。

## 数据库
- 不涉及数据库。

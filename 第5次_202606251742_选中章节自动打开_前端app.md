# 第5次提示词处理：选中章节自动打开

## 时间
2026-06-25 17:42

## 请求方
前端app

## 本次任务重点
移除“打开章节”按钮，改为章节或版本选择后自动生成并跳转页面。

## 下一步行动
- 刷新当前页面，确认顶部只保留章节下拉。
- 选择任一章节后，应自动显示“正在生成并打开所选章节”，随后跳转到对应渲染页。
- 若切换原文版本或修改稿版本，也应自动打开对应组合。

## 前端app
- 去除 `data-open-chapter` 按钮。
- 章节下拉、原文版本下拉、修改稿版本下拉变更后自动调用 `/api/build-chapter`。
- 初次加载目录时不自动跳转，避免页面循环生成。

## 管理后台
- 暂无管理后台改动。

## 后端
- 复用现有 `/api/build-chapter`，无接口变更。

## 数据库
- 不涉及数据库。

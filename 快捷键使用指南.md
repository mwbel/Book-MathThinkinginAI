# 🚀 Gemini 书稿修改快捷方式使用指南

## 📋 快捷方式总览

### 1. 命令行快捷工具

#### 基本命令
```bash
cd "/Users/Min369/Desktop/书/书稿打磨"

# 学术润色
python3 quick_edit.py polish --style academic --text "要润色的文本"

# 课堂风格润色
python3 quick_edit.py polish --style lecture --text "要润色的文本"

# 精炼文本
python3 quick_edit.py polish --style concise --text "要精炼的文本"

# 解释概念
python3 quick_edit.py explain --concept "概念名称"

# 生成例子
python3 quick_edit.py example --concept "概念名称"

# 翻译为中文
python3 quick_edit.py translate --text "英文文本"

# 检查严谨性
python3 quick_edit.py check --text "要检查的文本"

# 交互模式
python3 quick_edit.py interactive
```

#### 使用文件
```bash
# 处理整个文件
python3 quick_edit.py polish --style academic --file chapters/ch4/4-Thinking.tex

# 保存结果到新文件
python3 quick_edit.py polish --style academic --text "文本" --output result.txt
```

### 2. 超级快捷命令 (gedit)

使用 `./gedit` 进行快速操作：

```bash
./gedit p "文本"              # 学术润色
./gedit l "文本"              # 课堂风格润色
./gedit c "文本"              # 精炼文本
./gedit e "概念名称"          # 解释概念
./gedit x "概念名称"          # 生成例子
./gedit t "英文文本"          # 翻译中文
./gedit k "文本"              # 检查严谨性
./gedit i                     # 进入交互模式
./gedit h                     # 显示帮助
```

### 3. VS Code 快捷键

已配置以下快捷键（选中文本后使用）：

| 快捷键 | 功能 |
|--------|------|
| **Ctrl+Alt+P** | 学术风格润色 |
| **Ctrl+Alt+L** | 课堂风格润色 |
| **Ctrl+Alt+C** | 精炼文本 |
| **Ctrl+Alt+E** | 解释概念 |
| **Ctrl+Alt+X** | 生成例子 |
| **Ctrl+Alt+T** | 翻译中文 |

#### 使用方法：
1. 在 VS Code 中选中要处理的文本
2. 按对应的快捷键
3. 在终端查看处理结果

#### VS Code 任务面板：
- 按 `Ctrl+Shift+P`
- 输入 `Tasks: Run Task`
- 选择相应的编辑任务

## 💡 实际使用场景

### 场景1：快速润色段落
```bash
# 选中你的文本，然后运行：
./gedit p "旅行商问题是一个classical的NP完全问题。寻找访问n个城市的最短路径需要O(n!)的时间复杂度，当n较大时，即使是最快的计算机也无法在合理时间内求解。"
```

### 场景2：解释数学概念
```bash
# 解释复杂概念
./gedit e "近似算法"

# 生成具体例子
./gedit x "近似比"
```

### 场景3：处理文件
```bash
# 润色整个章节
python3 quick_edit.py polish --style academic --file "chapters/ch4/4-Thinking.tex" --output "chapters/ch4/4-Thinking_polished.tex"

# 检查数学严谨性
python3 quick_edit.py check --file "chapters/ch4/4-Thinking.tex"
```

### 场景4：交互式编辑
```bash
# 进入持续对话模式
python3 quick_edit.py interactive
```

## 🎯 书稿打磨专用

### 针对数学思维章节的优化：

```bash
# 将技术性内容改为课堂讲解风格
python3 quick_edit.py polish --style lecture --text "技术性描述"

# 精炼冗长的数学表达
python3 quick_edit.py polish --style concise --text "冗长的数学文本"

# 检查数学定义的严谨性
python3 quick_edit.py check --text "数学定义或定理"

# 为抽象概念生成例子
python3 quick_edit.py example --concept "NP完全问题"
```

### LaTeX 格式优化：
```bash
# 保持 LaTeX 格式的同时润色内容
python3 quick_edit.py polish --style academic --file "chapter.tex" --output "chapter_improved.tex"
```

## 🔧 配置文件位置

- **主配置**: `gemini_config.json`
- **快捷脚本**: `quick_edit.py`
- **超级命令**: `gedit`
- **VS Code 配置**: `.vscode/tasks.json`
- **使用统计**: `gemini_usage.json`

## 📊 监控和状态

```bash
# 查看API使用状态
python3 gemini_api_manager.py report

# 启动监控服务
python3 gemini_monitor.py monitor

# 测试所有API密钥
python3 gemini_api_manager.py test
```

## 🚨 注意事项

1. **选中文本**: 在 VS Code 中使用快捷键时确保已选中要处理的文本
2. **网络连接**: 确保网络连接稳定，API请求可能需要几秒钟
3. **API限额**: 系统会自动轮换API密钥，无需担心限额
4. **备份文件**: 处理重要文件时建议先备份

## 💬 获取帮助

```bash
# 查看快速编辑工具帮助
python3 quick_edit.py --help

# 查看超级命令帮助
./gedit h

# 查看 API 管理器帮助
python3 gemini_api_manager.py --help
```

---

🎉 **现在你可以使用这些快捷方式高效地修改书稿了！**
# AGENTS.md

## 项目目标

本项目为《人工智能的数学思维》书稿打磨主线工程（以 LaTeX 为主），目标是形成“原始稿 → 当前稿 → 干净校对稿/报告 → 差异可视化”的稳定闭环，并在 `tools/review` 中提供本地可用的校对前后端工作面。

核心链路（建议）：

1. 章节管理：`draft-tex-4-codex/chapters`，每章保持原始稿/当前稿/报告，并将中间产物归档到对应章目录。
2. 校对生成：`tools/review/run_review.py` 产出 `tools/review/outputs`。
3. 四栏对照预览：`synctex_review_server.py` 本地服务 + `review` 页面。
4. 质量回看：优先确认章节文本风格、逻辑结构、段间过渡、排版一致性。

## 执行前必须先输出 Preflight Plan

每次修改前，除非用户明确要求“直接改”或“先不改再观察”，必须先输出以下内容：

1. 本次任务目标。
2. 准备修改的文件。
3. 明确不会修改的文件。
4. 是否涉及以下高风险文件/区域：
   - `tools/review/run_review.py`
   - `tools/review/synctex_review_server.py`
   - `tools/review/scripts/*.sh`
   - `draft-tex-4-codex/chapters/`
   - `tools/review/outputs/`
   - 前后端渲染/同步逻辑
5. 预期执行命令。
6. 若涉及前端，必须给出最小冒烟测试步骤。

Preflight Plan 通过后可直接执行；仅当用户另行要求确认时暂停执行。

## 项目优先级

1. 保持章节文本结构与标签语法稳定，优先不改动已有 LaTeX 体系。
2. 保留现有前端核心展示流的最小可用行为，不做非必要 UI 破坏性改动。
3. 优先修复可复用、低风险修复项。
4. 仅在必要时新增脚本/文档，不轻易改造既有目录约定。

## 禁止越界修改（除非明确授权）

1. 不直接大规模改写 `chapters` 全量内容。
2. 不删除或覆盖尚未沟通确认的历史版本。
3. 不修改第三方依赖和外部 API 配置。
4. 不修改未说明的 `.git` 相关策略与历史清理流程。
5. 不无理由引入新的目录约定或重命名关键文件。

## 最小交付规则

1. 章节文本修改优先保持原始缩进与空行结构。
2. 代码/脚本改动必须说明原因并限定最小影响范围。
3. 新增文件必须在同一任务中说明用途和预期生命周期。
4. 校对页面相关改动需提供可重现打开方式（端口、页面路径）。

## 提示词归档规则（必须执行）

每次收到提示词，都按以下规则写入本项目文档：

1. 若 `提示词历史.md` 不存在则创建；每条按以下格式追加：
   - 对话时间
   - 第几次给提示词（本项目内序号）
   - 本次提示词完整内容

2. 同步创建一个汇总文档：
   - 文件名格式：`第N次_YYYYMMDDHHMM_任务重点20字内_请求方.md`
   - `N` 为本项目内第N次提示词（与提示词历史一致）
   - `YYYYMMDDHHMM` 使用完整到分钟的时间
   - `任务重点` 不超过20个字，表达本次核心任务
   - `请求方` 从 `前端app`、`管理后台`、`后端`、`数据库` 中选择（仅填写一个）

3. 汇总文档必须只放在一个 md 文件中，至少包含三项：
   - 本次完成的任务重点
   - 下一步行动
   - 前端app、管理后台、后端、数据库的下一步（每项单独列出）

4. 不要生成“前端app.md / 管理后台.md / 后端.md / 数据库.md”四分离文档。

## 最低质量检查

```bash
cd "/Users/Min369/Desktop/书/书稿打磨"
git status --short
git diff --check
# 如涉及前后端联调
python3 tools/review/scripts/start_review_backend.sh
# 或
./tools/review/scripts/start_review_all.sh
```

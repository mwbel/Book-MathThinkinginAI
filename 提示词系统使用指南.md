# 🎯 提示词系统使用指南

## 📋 系统概述

我们已经为你创建了一个完整的提示词管理系统，让AI输出更符合你的《数学思维与AI》书稿风格。

## 🚀 快速开始

### 1. 查看所有可用提示词
```bash
python3 quick_edit.py prompts --list-prompts
```

### 2. 使用内置提示词
```bash
# 学术风格润色（使用专业提示词）
python3 quick_edit.py polish --style academic --text "你的文本"

# 数学概念解释
python3 quick_edit.py explain --concept "旅行商问题"

# 生成章节引言
python3 quick_edit.py intro --chapter 4 --title "数学思维"
```

### 3. 使用自定义提示词
```bash
# 添加自定义提示词
python3 prompt_manager.py add --name "我的提示词" --template "你的模板"

# 使用自定义提示词
python3 quick_edit.py custom --prompt-name "我的提示词" --text "处理文本"
```

## 📚 提示词类别

### 1. 文本润色类 (`polish`)

| 提示词 | 用途 | 特点 |
|--------|------|------|
| `academic` | 学术风格润色 | 严谨、规范、符合学术写作 |
| `lecture` | 课堂讲解风格 | 生动、易懂、适合教学 |
| `concise` | 精炼文本 | 删除冗余、保留核心 |
| `expand` | 详细扩写 | 增加背景、补充例子 |

**使用示例：**
```bash
python3 quick_edit.py polish --style lecture --text "复杂的技术描述"
```

### 2. 数学内容处理 (`math`)

| 提示词 | 用途 | 输出特点 |
|--------|------|----------|
| `explain_concept` | 解释数学概念 | 定义+性质+例子+应用 |
| `generate_examples` | 生成数学例子 | 基础→中等→高级 |
| `check_rigor` | 检查严谨性 | 指出问题+改进建议 |
| `improve_formula` | 公式解释改进 | 符号含义+推导过程 |

**使用示例：**
```bash
python3 quick_edit.py explain --concept "NP完全问题"
python3 quick_edit.py formula --formula "T(n) = O(n!)" --context "时间复杂度"
```

### 3. 书稿专用 (`book_specific`)

| 提示词 | 用途 | 适用场景 |
|--------|------|----------|
| `chapter_intro` | 章节引言 | 新章节开始 |
| `example_enhancement` | 例子增强 | 丰富现有例子 |
| `exercise_generation` | 习题生成 | 章节练习 |

**使用示例：**
```bash
python3 quick_edit.py intro --chapter 5 --title "算法优化"
python3 quick_edit.py exercise --concept "动态规划"
```

### 4. 翻译类 (`translation`)

| 提示词 | 用途 | 特点 |
|--------|------|------|
| `to_chinese` | 英译中 | 专业术语准确 |
| `to_english` | 中译英 | 学术英语表达 |

## 🎨 自定义提示词

### 创建专业提示词模板
```python
# 添加书稿专用提示词
python3 prompt_manager.py add \
  --name "数学概念润色" \
  --template "请以《数学思维与AI》的写作风格，将以下数学概念表述得更通俗易懂：
要求：
1. 用生活化的比喻解释抽象概念
2. 保持数学表述的准确性
3. 语言像老师在课堂讲解
4. 适当加入历史背景
概念：{text}"
```

### 提示词模板语法
使用 `{变量名}` 作为占位符：
- `{text}` - 要处理的文本
- `{concept}` - 数学概念
- `{formula}` - 数学公式
- `{context}` - 上下文信息
- `{chapter_number}` - 章节编号
- `{chapter_title}` - 章节标题

## 💡 实际应用案例

### 案例1：润色教科书内容
```bash
# 原文：旅行商问题是classical的NP完全问题
python3 quick_edit.py polish --style academic --text "旅行商问题是classical的NP完全问题"

# 输出：Traveling Salesperson Problem (TSP) is a canonical instance...
```

### 案例2：生成教学讲解
```bash
# 为抽象概念生成生动解释
python3 quick_edit.py explain --concept "近似算法"

# 输出：包含严格定义、直观含义、具体例子的完整讲解
```

### 案例3：创建章节引言
```bash
# 为第4章生成引人入胜的引言
python3 quick_edit.py intro --chapter 4 --title "数学思维"

# 输出：以AI应用切入，引出数学思维重要性的生动引言
```

### 案例4：使用自定义风格
```bash
# 先添加自定义提示词
python3 prompt_manager.py add --name "课堂风格" --template "请像老师讲课一样..."

# 然后使用
python3 quick_edit.py custom --prompt-name "课堂风格" --text "复杂理论"
```

## 🔧 提示词管理

### 查看和管理
```bash
# 列出所有类别
python3 prompt_manager.py list

# 列出特定类别的提示词
python3 prompt_manager.py list --category math

# 搜索提示词
python3 prompt_manager.py search --keyword "公式"

# 查看提示词示例
python3 prompt_manager.py examples --category polish --name academic
```

### 自定义提示词管理
```bash
# 添加提示词
python3 prompt_manager.py add --name "提示词名" --template "模板内容"

# 获取提示词模板
python3 prompt_manager.py get --name "提示词名"

# 搜索提示词
python3 prompt_manager.py search --keyword "关键词"
```

## 📊 提示词效果对比

### 使用前 vs 使用后

| 文本类型 | 使用前 | 使用后 |
|----------|--------|--------|
| 学术概念 | 简单描述 | 严谨定义+详细解释+实例 |
| 技术文本 | 枯燥技术性 | 课堂讲解风格+生动比喻 |
| 数学公式 | 符号堆砌 | 符号含义+推导过程+应用 |
| 章节引言 | 平铺直叙 | 引人入胜+激发兴趣 |

## 🎯 最佳实践

### 1. 选择合适的提示词
- **学术论文**: 使用 `academic` 风格
- **课堂教学**: 使用 `lecture` 风格
- **概念解释**: 使用 `explain_concept`
- **例子丰富**: 使用 `example_enhancement`

### 2. 组合使用提示词
```bash
# 先润色，再检查严谨性
python3 quick_edit.py polish --style academic --text "内容"
python3 quick_edit.py check --text "润色后的内容"
```

### 3. 创建个人化提示词
根据你的写作风格和需求，创建专属提示词模板。

### 4. 渐进式优化
1. 先用基础提示词生成初稿
2. 再用特定提示词优化细节
3. 最后用严谨性检查确保质量

## 📋 快捷命令汇总

| 命令 | 功能 | 示例 |
|------|------|------|
| `prompts` | 管理提示词 | `python3 quick_edit.py prompts --list-prompts` |
| `polish` | 文本润色 | `python3 quick_edit.py polish --style academic` |
| `explain` | 概念解释 | `python3 quick_edit.py explain --concept "动态规划"` |
| `intro` | 章节引言 | `python3 quick_edit.py intro --chapter 4 --title "数学思维"` |
| `exercise` | 习题生成 | `python3 quick_edit.py exercise --concept "贪心算法"` |
| `custom` | 自定义提示词 | `python3 quick_edit.py custom --prompt-name "我的提示词"` |

---

🎉 **现在你可以使用强大的提示词系统，让AI输出完全符合你的书稿风格了！**
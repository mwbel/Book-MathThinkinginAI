# 第3次提示词处理：修复打开章节报错

## 时间
2026-06-25 17:27

## 请求方
前端app

## 本次任务重点
修复点击“打开章节”时仍调用旧 TeX 根目录导致报错的问题。

## 下一步行动
- 使用启动脚本重新启动本地 review 服务，确保 8766 端口上的旧后端被替换。
- 若页面仍显示旧章节名，刷新页面后重新选择章节并点击“打开章节”。
- 后续可在页面顶部显示当前后端章节目录，减少误判。

## 前端app
- 启动脚本默认重启旧服务，避免旧版后端继续响应“打开章节”请求。

## 管理后台
- 暂无管理后台改动。

## 后端
- `start_review_backend.sh` 与 `start_review_daemon.sh` 默认清理 8766 端口旧服务后再启动。
- 后端对 TeX 根目录错误给出更短、更可操作的提示。

## 数据库
- 不涉及数据库。

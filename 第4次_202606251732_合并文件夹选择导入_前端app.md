# 第4次提示词处理：合并文件夹选择导入

## 时间
2026-06-25 17:32

## 请求方
前端app

## 本次任务重点
修复文件夹导入失败，并将“导入文件夹”合并到“选择”动作中。

## 下一步行动
- 重新生成渲染页，确保新页面脚本生效。
- 用 `http://127.0.0.1:8766` 打开页面，不要用 `file://` 直接打开旧 HTML。
- 点击“选择”后应自动导入章节目录并刷新章节下拉。

## 前端app
- 移除单独“导入文件夹”按钮。
- “选择”文件夹后自动导入。
- 手动输入路径后按回车或失焦自动导入。
- 对 `Failed to fetch` 给出本地服务启动提示。

## 管理后台
- 暂无管理后台改动。

## 后端
- 导入接口支持输入 `draft-tex-4-codex` 这类书稿根目录，并自动定位到其 `chapters` 子目录。
- 当目录不存在或没有章节 tex 时返回更明确错误。

## 数据库
- 不涉及数据库。

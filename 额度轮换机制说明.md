# ⚠️ 重要澄清：Gemini API 额度轮换机制

## 🔍 关键发现

**Gemini API 的免费额度是按 Google Cloud 项目计算的，不是按 API key！**

这意味着：
- ❌ **错误理解**：每个 API key 有独立的 250K tokens/天
- ✅ **正确理解**：整个项目共享 250K tokens/天

## 📊 实际情况分析

### 当前系统现状
- 你的 9 个 API keys **都属于同一个 Google Cloud 项目**
- 它们**共享同一个** 250K tokens/天的额度
- 当一个 key 用完额度，其他 key 也会受影响

### 轮换机制的实际作用

虽然不能增加总额度，但轮换系统仍然有价值：

#### ✅ 1. **容错备份**
```
场景：某个 API key 出现问题
├── Key-1 网络超时 → 自动切换到 Key-2
├── Key-2 服务器错误 → 自动切换到 Key-3
└── Key-3 正常工作 ✅
```

#### ✅ 2. **负载均衡**
```
场景：避免单点过载
├── 分散请求到不同 keys
├── 降低单个 key 的请求频率
└── 提高系统稳定性
```

#### ✅ 3. **速率限制优化**
```
场景：应对分钟级限制
├── RPM: 10次/分钟 (项目级)
├── 轮换多个 key 可以更充分利用
└── 避免单个 key 触发限制
```

## 🔄 修正后的轮换策略

### 1. **项目级额度管理**
- 跟踪整个项目的总使用量
- 在接近 250K tokens 时发出预警
- 北京时间下午3点自动重置

### 2. **智能 API key 选择**
- 轮询使用可用的 keys
- 避免频繁使用同一个 key
- 自动跳过出现问题的 key

### 3. **错误恢复机制**
- 429 错误（额度超限）：标记所有 keys 为冷却
- 403 错误（key 无效）：永久禁用该 key
- 网络错误：尝试下一个 key

## 💡 实际使用建议

### ✅ 推荐做法
1. **监控项目总使用量**，而不是单个 key
2. **在北京时间下午3点后**开始大量使用
3. **合理分配请求**到不同 keys 以提高稳定性
4. **设置预警机制**，避免超出限额

### ❌ 避免误解
1. 不要期望 9 个 keys = 9 × 250K tokens
2. 不要在项目额度用完后继续尝试其他 keys
3. 不要忽略项目级的速率限制

## 🛠️ 系统改进

基于这个发现，我们需要：

1. **修改额度计算逻辑**：项目级而非 key 级
2. **更新使用报告**：显示项目总使用量
3. **优化轮换策略**：重点在容错而非增加额度

---

**结论**：轮换系统仍然有价值，但主要用于**容错和负载均衡**，而不是增加总额度。
# 第2次提示词处理：修复章节渲染错配

## 时间
2026-06-25 16:19

## 请求方
前端app

## 本次任务重点
修复章节选择、章节名称与实际渲染页面不一致的问题。

## 下一步行动
- 重新生成或打开目标章节页面，确认下拉章节、页面标题、源码与渲染稿属于同一章。
- 若旧的 `ch12-clean-render.html` 仍被手动打开，页面应提示章节目录不匹配，而不是自动选中错误章节。
- 后续可补一个轻量自检脚本，检查当前 HTML 内的源文件路径是否存在于 `/api/chapters` 返回的目录中。

## 前端app
- 章节目录选择不再在源文件不匹配时自动回退到第一个章节。
- 启动脚本默认打开最近生成的 `*-render.html`，避免固定打开旧的 ch12 页面。

## 管理后台
- 暂无管理后台改动。

## 后端
- 默认章节目录切换到 `draft-tex-4-codex/chapters`。
- 生成章节时自动向上寻找包含 `elegantbook.cls` 的 TeX 根目录。

## 数据库
- 不涉及数据库。
